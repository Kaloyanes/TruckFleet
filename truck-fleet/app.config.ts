export default defineAppConfig({
  ui: {
    primary: 'purple',
    gray: 'neutral',
    notifications: {
      // Show toasts at the top right of the screen
      position: 'top-auto bottom-0'
    },
    icons: {
      dynamic: true,
    },
  }
})
