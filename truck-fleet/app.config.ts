export default defineAppConfig({
  ui: {
    primary: 'indigo',
    gray: 'neutral',
    notifications: {
      // Show toasts at the top right of the screen
      position: 'top-auto bottom-0'
    },
    icons: {
      dynamic: true,
    }
  }
})
